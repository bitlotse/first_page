<article class="service-card">
  <img src="{{ icon }}" alt="{{ title }} Icon" class="service-icon" />
  <h2>{{ title }}</h2>
  <p>{{ description }}</p>
  <a href="{{ url }}" class="btn">Mehr erfahren</a>
</article>
